export default function Footer() {
  return (
    <footer className="w-full h-0 opacity-0 overflow-hidden" aria-hidden="true">
      {/* Footer is intentionally empty to allow Testimonials to act as the final section */}
    </footer>
  );
}